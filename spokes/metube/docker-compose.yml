services:
  metube:
    image: alexta69/metube:latest
    container_name: metube
    restart: unless-stopped
    volumes:
      - ./data:/downloads
    labels:
      traefik.enable: true
      traefik.http.routers.metube.rule: Host(`ytdownload.${DOMAIN_NAME}`)
      traefik.http.routers.metube.entrypoints: websecure
      traefik.http.routers.metube.tls: true
      traefik.http.routers.metube.tls.certresolver: cloudflare
      traefik.http.routers.metube.middlewares: authentik@docker
      homepage.group: Apps
      homepage.href: https://ytdownload.${DOMAIN_NAME}
      homepage.icon: metube
      homepage.name: Youtube Downloader
    networks:
      - traefik-frontend

networks:
  traefik-frontend:
    external: true
